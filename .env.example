# ===========================================
# Browser Automation Environment Variables
# ===========================================

# Google Gemini API Key (Required)
# Used by the AI SDK to analyze screenshots and extract structured data
# from flight search results and ride information.
# Get your API key at: https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key_here

# Headful Browser Mode (Optional)
# Set to "true" to run Playwright in headed (visible) mode.
# Useful for:
#   - Debugging automation scripts
#   - Manual login flows (e.g., Uber authentication)
#   - Watching the automation in action
# Leave unset or "false" for headless mode (faster, no UI).
HEADFUL=true
